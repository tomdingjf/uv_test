[project]
name = "pyproject.toml"
description = "A simple project to help you get started quickly with building new Python projects using Python."
version = "0.1.0"
dependencies = [
    "flask>=3.1.3",
    "lxml>=6.0.2",
    "pandas>=3.0.2",
    "requests>=2.33.1",
]
